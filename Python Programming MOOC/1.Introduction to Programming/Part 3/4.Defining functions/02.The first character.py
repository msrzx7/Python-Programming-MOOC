def first_character(text): # function with argument inside
    print(first_character[0]) 
    # arugument working as parameter/ variable to store the input

first_character() # calling function

'''Note: argument is the value given to the funciton and parameter 
is the placeholder which works as variable to store the argument.
A function can take one or more argument.'''